# 使用指南（人类版）

> 欢迎来到 **AI 全民制作人** —— 一个收集 AI 生成的网页 Demo 的展示站。
> 你可以在这里逛作品、看 AI 的生成过程、给作品打分吐槽，也可以自己上传 AI 做的 Demo。

> ✨ **亮点：提示词模式** —— 在作品库切到「提示词」视图，直接看每个作品的第一轮提示词，看 AI 是怎么被“一句话点单”的。

---

## 1. 快速开始

- **逛作品**：打开首页看精选，或进「作品库」搜索、按标签筛选、排序。
- **看详情**：点进任意 Demo，可以：
  - 在线预览（网页类）
  - 下载文件包
  - 看「信息 / 时间线 / 会话日志 / 评论」四个 Tab
  - 给作品评分、写评论
- **参与**：注册登录后可以上传自己的作品、管理自己的作品、评论互动。

---

## 2. 账号与登录

- 右上角「登录 / 注册」。
- 注册：用户名（字母数字下划线）+ 密码（至少 8 位）。
- 登录后：
  - 个人主页 `/user/你的用户名` 展示你的作品
  - 「设置」页可以修改密码
- **不登录也能上传**，但作者会显示为 `public`，且不能评论、不能编辑/删除自己的作品（只有管理员能管）。

---

## 3. 上传作品

入口：导航栏「上传 Demo」。

### 3.1 作品类型

| 类型 | 说明 | 要求 |
|---|---|---|
| **网页应用（web）** | 在线 iframe 预览 | 上传 zip（根目录含 `index.html`），或单个 `.html` / `.svg` |
| **文件包（zip）** | 只提供下载，不预览 | 上传 zip（不需要 index.html） |
| **外部链接（link）** | 跳转到站外 | 填 `external_url`（http/https） |

### 3.2 填写信息

- **标题**：必填，写清楚作品名。
- **描述**：建议写 2~4 句，说明这是什么、怎么玩。
- **标签**：从标签系统里选（见第 6 节）。
- **提示词（可选）**：生成这个 Demo 的第一轮提示词，会展示在详情页。
- **视频链接（可选）**：B站/YouTube 等介绍视频。
- **封面（可选）**：自动压缩为 WebP，不用自己处理尺寸。

### 3.3 匿名上传

- 不登录也能传，作者固定为 `public`。
- 匿名上传默认**不会自动公开**，需要管理员审核通过后才上线。
- 如果站点配置了信任通道（`UPLOAD_CODE`），带正确 code 的匿名上传可以免审核直接上线（主要给 AI agent 用）。

### 3.4 上传后

- 上传成功会得到作品页链接。
- 状态：
  - `approved`：已上线，大家都能看到
  - `pending`：等待管理员审核
  - `rejected`：被管理员拒绝（可修改后重新上传）

---

## 4. 管理自己的作品

登录后，进入自己的作品详情页，可以：

- **编辑**：改标题/描述/标签/封面/文件，写更新说明。
- **保留旧版本**：编辑时勾选「保留旧版本」，当前版本会存为一个独立旧版页面，方便对比。
- **删除**：删除自己的作品（会同时清理文件）。

每个作品都有**时间线**，记录创建、更新、旧版快照，过程全透明。

---

## 5. 互动

### 5.1 评分

- 详情页有 1~5 星评分：
  - 5 = 神作
  - 4 = 佳作
  - 3 = 一般
  - 2 = 差
  - 1 = 鬼作
- 登录用户：一个作品一票，可以改分/撤分。
- 匿名用户：浏览器会生成匿名设备标识，也可以评分/改分/撤分（有频率限制）。

### 5.2 评论

- 详情页「评论」Tab 可以发表评论、回复别人（树形，深度最多 5 层）。
- 登录用户才能评论；`public` 匿名上传者不能评论。

### 5.3 排行榜

- 导航栏「排行榜」：
  - 按平均分、神作票数、鬼作票数、神鬼净票、评分人数、热度排序。
  - 只有已上线的作品参与排行。

---

## 6. 标签系统

标签用来给作品分类，方便查找。

- **固定值（fixed）**：管理员维护的候选值，直接点选，如 `model:DeepSeek-V4`。
- **开放值（open）**：key 固定，value 自己填，如 `game:mc`。
- **数字值（int）**：填整数，如 `rounds:3`，支持范围筛选（`rounds:3-10`）。

### 申请新固定值

如果上传时发现想要的固定值不存在：

1. 在标签选择器点「申请新固定值」。
2. 填写 key / value / 说明。
3. 管理员审核通过后，这个值就会成为正式候选值。

---

## 7. 关于本站

「关于」页可以看到：

- 站点访问统计（今日 / 昨日 / 总 PV、实时在线）
- 赞助榜
- 致谢榜

---

## 8. 管理后台（仅管理员）

管理员登录后，导航栏会出现「管理后台」，可以：

- 审核待上线的 Demo
- 管理所有 Demo（编辑/删除）
- 管理用户（角色/状态）
- 管理标签键与固定值、审核标签申请
- 发布/管理公告
- 管理赞助/致谢
- 查看存储状态、触发 OSS 同步
- 修改站点设置（自动审核开关）

---

## 9. 小贴士

- **搜索**：作品库支持按标题/描述搜索，也支持多标签组合筛选。
- **提示词模式**：作品库可以切到「提示词」视图，专门看带提示词的作品。
- **相关推荐**：详情页底部有「相关推荐」，可以换一批。
- **会话日志**：如果作品上传时带了 DSH 会话日志（`.jsonl`），详情页会以轨迹形式展示 AI 的生成过程。
- **单文件作品**：`.html` / `.svg` 可以直接上传，下载时返回原文件，不打包 zip。

---

*有问题？找管理员。玩得开心！*
