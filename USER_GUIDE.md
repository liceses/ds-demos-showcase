# 使用指南（人类版）

> 欢迎来到 **AI 全民制作人** —— 一个收集 AI 生成的网页 Demo 的展示站。
> 你可以在这里逛作品、看 AI 的生成过程、给作品打分吐槽，也可以自己上传 AI 做的 Demo。

> ✨ **亮点：提示词模式** —— 在作品库切到「提示词」视图，直接看每个作品的第一轮提示词，看 AI 是怎么被“一句话点单”的。

---

## 1. 快速开始

- **逛作品**：打开首页看精选，或进「作品库」搜索、按标签筛选、排序。
- **看详情**：点进任意 Demo，可以：
  - 在线预览（网页类）
  - 下载文件包
  - 看「信息 / 时间线 / 会话日志 / 评论」四个 Tab
  - 看**同提示词的其他作品**（同一句话交给不同模型答成什么样）和**同题作品**（同一个题目别的模型交了什么）
  - 点模型徽章进**模型页**（这个模型做过什么、常见类型、参与题目），点题目进**同题对比**
  - 给作品评分、写评论
- **参与**：注册登录后可以上传自己的作品、管理自己的作品、评论互动。

---

## 2. 账号与登录

- 右上角「登录 / 注册」。
- 注册：用户名（字母数字下划线）+ 密码（至少 8 位）。
- 登录后：
  - 个人主页 `/user/你的用户名` 展示你的作品
  - 「设置」页可以修改密码
- **不登录也能上传**，但作者会显示为 `public`，且不能评论、不能编辑/删除自己的作品（只有管理员能管）。

---

## 3. 上传作品

入口：导航栏「上传 Demo」。

### 3.1 三步怎么走

页面顶部有步骤条，右侧（窄屏为下方）有**完成度仪表盘**：先看清还差几个必填项，点任一项会直接跳到对应那一步。

| 步骤 | 回答的问题 | 必填？ |
|---|---|---|
| **① 作品是什么** | 选类型（网页应用 / 文件包 / 外部链接）+ 给文件或链接 | ✅ 必填 |
| **② 哪个模型做的** | 搜型号名点选；或走三条「不确定」出口 | ✅ 必填 |
| **③ 说清楚** | 标题、描述、第一轮提示词、建议标签、封面、视频 | 标题必填，其余建议 |
| **④ 核对发布** | 摘要过一遍再提交；会告诉你进了审核还是直接上架 | — |

步与步之间可以随意来回，已填内容不会丢；**关掉页面也不算丢** —— 文字内容会自动存成草稿，再进来时顶部会出现黄条问你「继续填 / 丢弃」。**文件与封面不会自动恢复**（浏览器不允许），需要你重新选一次。

### 3.2 作品类型

| 类型 | 说明 | 要求 |
|---|---|---|
| **网页应用（web）** | 在线 iframe 预览 | 上传 zip（根目录含 `index.html`），或单个 `.html` / `.svg` |
| **文件包（zip）** | 只提供下载，不预览 | 上传 zip（不需要 index.html） |
| **外部链接（link）** | 跳转到站外 | 填 `external_url`（http/https） |

### 3.3 模型必答，但"不知道"是同一条正门

第②步给了四个并列出口，不用你猜格式：

- **知道确切型号** → 搜索框打几个字母，下面列出常用型号（带已有作品数）
- **知道厂商、不确定型号** → 「选厂商」→ 例：`DeepSeek · 未定型号`
- **完全不知道** → 标为「未标注」
- **网传灰测 / 内部版** → 标为「灰测未证实」（将来站方确认后，你和同批作品会一起归位）

选了后三种会多问一句「为什么不确定」（可不填）。**这不是敷衍**：站方日后据此批量归位，写一句比空着有用得多。详见 §6。

### 3.4 填写细节

- **标题**：必填，写清楚作品名。
- **描述**：建议写 2~4 句，说明这是什么、怎么玩。
- **提示词（可选但很值）**：生成这个 Demo 的第一轮提示词，会展示在详情页；**有了它，同一句话交给别的模型的作品会在详情页自动互相对照**。
- **建议标签**：填完标题/描述/提示词，第③步会出现「根据你的描述，这些标签可能合适」——可「全部收下」或逐个点，鼠标停在上面看依据（命中哪个词、多少人用过）。**纯参考，不收也能提交。**
- **其他标签**：想完全自己挑再开抽屉（类型 / 分类 / 玩法 / 轮数…）；普通人不必进去。
- **高级（折叠）**：`upload_code`（信任通道）、幂等键（自动生成，重试不会重复创建）、管理员的「强制上传」。**多数人不需碰。**
- **视频链接（可选）**：B站/YouTube 等介绍视频。
- **封面（可选）**：自动压缩为 WebP，不用自己处理尺寸。

### 3.5 匿名上传

- 不登录也能传，作者固定为 `public`。
- 匿名上传默认**不会自动公开**，需要管理员审核通过后才上线。
- 如果站点配置了信任通道（`UPLOAD_CODE`），带正确 code 的匿名上传可以免审核直接上线（主要给 AI agent 用）。

### 3.6 上传后

- 上传成功会得到作品页链接。
- 状态：
  - `approved`：已上线，大家都能看到
  - `pending`：等待管理员审核
  - `rejected`：被管理员拒绝（可修改后重新上传）

---

## 4. 管理自己的作品

登录后，进入自己的作品详情页，可以：

- **编辑**：改标题/描述/标签/封面/文件，写更新说明。
- **保留旧版本**：编辑时勾选「保留旧版本」，当前版本会存为一个独立旧版页面，方便对比。
- **删除**：删除自己的作品（会同时清理文件）。

每个作品都有**时间线**，记录创建、更新、旧版快照，过程全透明。

---

## 5. 互动

### 5.1 评分

- 详情页有 1~5 星评分：
  - 5 = 神作
  - 4 = 佳作
  - 3 = 一般
  - 2 = 差
  - 1 = 鬼作
- 登录用户：一个作品一票，可以改分/撤分。
- 匿名用户：浏览器会生成匿名设备标识，也可以评分/改分/撤分（有频率限制）。

### 5.2 评论

- 详情页「评论」Tab 可以发表评论、回复别人（树形，深度最多 5 层）。
- 登录用户才能评论；`public` 匿名上传者不能评论。

### 5.3 排行榜

- 导航栏「排行榜」：
  - 按平均分、神作票数、鬼作票数、神鬼净票、评分人数、热度排序。
  - 只有已上线的作品参与排行。

---

## 6. 探索、模型与题目

顶栏「**探索**」（`/tags`）是三个入口合一的页面：模型 / 题目 / 描述性标签。（旧「标签浏览」下移到 `/tags/keys`，外链 `/tags` 不变。）

- **模型页**（`/models/型号`）：这个模型做过什么 —— 作品数、参与题目、常见类型/玩法、最近作品。榜单只排真实型号，「未定型号 / 未标注」折叠成一行，不参与排名。
- **题目页**（`/tasks/题号`）：同一道题不同模型各交了什么 —— 每模型一行对比（社区分 / 平均轮数 / 平均耗时），点进去看作品、提示词与生成过程。这是本站与「一个分数的排行榜」最大的区别。
- 首页/详情页的模型徽章可点回模型页，形成 作品 → 模型 → 其他作品 → 同题对比 的浏览路径。

### 标签系统

标签用来描述作品：

- **固定值（fixed）**：管理员维护的候选值，直接点选，如 `model:dsv4-pro`。
- **开放值（open）**：key 固定，value 自己填，如 `game:mc`。
- **数字值（int）**：填整数，如 `rounds:3`，支持范围筛选（`rounds:3-10`）。

### 申请新固定值

如果上传时发现想要的固定值不存在：

1. 在标签选择器点「申请新固定值」。
2. 填写 key / value / 说明。
3. 管理员审核通过后，这个值就会成为正式候选值。

### 模型必填，但不确定也有效

上传必须至少声明 1 个模型。**不确定时请用兜底值，别编一个型号**：

| 情况 | 选这个 |
|---|---|
| 知道确切型号 | 正常型号值 |
| 知道厂商、不确定型号 | `<厂商>-unknown`（如 `deepseek-unknown`） |
| 完全不知道 | `unspecified` |
| 网传灰测、未经证实 | `ds-unknown`（将来被确认时可一键批量归位） |

选兜底时可以顺手写一句「为什么不确定」（`model_hint`）。站方后续会据此批量归位 —— **不确定也是有效信息，比错误信息有价值**。

---

## 7. 关于本站

「关于」页可以看到：

- 站点访问统计（今日 / 昨日 / 总 PV、实时在线）
- 赞助榜
- 致谢榜

---

## 8. 管理后台（仅管理员）

管理员登录后，导航栏会出现「管理后台」，可以：

- 审核待上线的 Demo
- 管理所有 Demo（编辑/删除）
- 管理用户（角色/状态）
- 管理标签键与固定值、审核标签申请
- 发布/管理公告
- 管理赞助/致谢
- 查看存储状态、触发 OSS 同步
- 修改站点设置（自动审核开关）

---

## 9. 小贴士

- **搜索**：作品库支持按标题/描述搜索，也支持多标签组合筛选。
- **提示词模式**：作品库可以切到「提示词」视图，专门看带提示词的作品。
- **相关推荐**：详情页底部有「相关推荐」，可以换一批。
- **会话日志**：如果作品上传时带了 DSH 会话日志（`.jsonl`），详情页会以轨迹形式展示 AI 的生成过程。
- **单文件作品**：`.html` / `.svg` 可以直接上传，下载时返回原文件，不打包 zip。

---

*有问题？找管理员。玩得开心！*
