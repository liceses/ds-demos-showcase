const CDP=9333;const l=await (await fetch("http://127.0.0.1:"+CDP+"/json/list")).json();const ws=new WebSocket(l.find(t=>t.type==="page").webSocketDebuggerUrl);await new Promise(r=>ws.onopen=r);let seq=0;const pend=new Map();ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.id&&pend.has(m.id)){const p=pend.get(m.id);pend.delete(m.id);m.error?p.rej(new Error(JSON.stringify(m.error))):p.res(m.result)}};const send=(m,p={})=>new Promise((res,rej)=>{const id=++seq;pend.set(id,{res,rej});ws.send(JSON.stringify({id,method:m,params:p}))});const ev=async e=>{const r=await send("Runtime.evaluate",{expression:e,returnByValue:true,awaitPromise:true});if(r.exceptionDetails)throw new Error(JSON.stringify(r.exceptionDetails).slice(0,250));return r.result.value};await send("Page.enable");const out={};out.win=await ev("({inner:innerWidth,outer:outerWidth,docW:document.documentElement.clientWidth})");await send("Page.navigate",{url:"http://localhost:5173/demo-c004ab5x"});await new Promise(r=>setTimeout(r,4000));out.nf=await ev("(()=>({path:location.pathname,is404View:document.body.textContent.includes(\"404\"),guessEl:!!document.querySelector(\".nf-guess\"),guessText:(document.querySelector(\".nf-guess\")||{}).textContent?.trim().slice(0,100)||null,entries:[\"/demos\",\"/tags\",\"/leaderboard\",\"/forum\"].map(h=>!!document.querySelector(`a[href=\"${h}\"]`))}))()");console.log(JSON.stringify(out,null,1));ws.close();process.exit(0)