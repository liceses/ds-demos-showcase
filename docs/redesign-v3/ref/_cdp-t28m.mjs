const CDP=9333;const l=await (await fetch("http://127.0.0.1:9333/json/list")).json();const ws=new WebSocket(l.find(t=>t.type==="page").webSocketDebuggerUrl);await new Promise(r=>ws.onopen=r);let seq=0;const pend=new Map();ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.id&&pend.has(m.id)){const p=pend.get(m.id);pend.delete(m.id);m.error?p.rej(new Error(JSON.stringify(m.error))):p.res(m.result)}};const send=(m,p={})=>new Promise((res,rej)=>{const id=++seq;pend.set(id,{res,rej});ws.send(JSON.stringify({id,method:m,params:p}))});const ev=async e=>{const r=await send("Runtime.evaluate",{expression:e,returnByValue:true,awaitPromise:true});if(r.exceptionDetails)throw new Error(JSON.stringify(r.exceptionDetails).slice(0,250));return r.result.value};const fs=await import("node:fs");await send("Page.enable");await send("Page.navigate",{url:"http://localhost:5173/demos"});await new Promise(r=>setTimeout(r,1800));await ev("localStorage.setItem(\"dsh_demos_facet_pin\",\"1\")");await send("Page.reload");await new Promise(r=>setTimeout(r,2600));const r=await ev("(()=>({ls:localStorage.getItem(\"dsh_demos_facet_pin\"),vw:innerWidth,pinnedPanel:!!document.querySelector(\".facet-panel--pinned\"),sticky:(()=>{const p=document.querySelector(\".facet-panel--pinned\");return p?getComputedStyle(p).position:null})(),btnGone:!!document.querySelector(\".facet-btn\"),grammar:document.body.textContent.includes(\"跨组叠加\")}))()");const s=await send("Page.captureScreenshot",{format:"png"});fs.writeFileSync("D:/developing/ds民间科研成果展示/web/docs/redesign-v3/ref/t28-pin.png",Buffer.from(s.data,"base64"));console.log(JSON.stringify(r,null,1));ws.close();process.exit(0)