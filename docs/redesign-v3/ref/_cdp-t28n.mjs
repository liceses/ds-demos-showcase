const CDP=9333;const l=await (await fetch("http://127.0.0.1:9333/json/list")).json();const ws=new WebSocket(l.find(t=>t.type==="page").webSocketDebuggerUrl);await new Promise(r=>ws.onopen=r);let seq=0;const pend=new Map();ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.id&&pend.has(m.id)){const p=pend.get(m.id);pend.delete(m.id);m.error?p.rej(new Error(JSON.stringify(m.error))):p.res(m.result)}};const send=(m,p={})=>new Promise((res,rej)=>{const id=++seq;pend.set(id,{res,rej});ws.send(JSON.stringify({id,method:m,params:p}))});const ev=async e=>{const r=await send("Runtime.evaluate",{expression:e,returnByValue:true,awaitPromise:true});if(r.exceptionDetails)throw new Error(JSON.stringify(r.exceptionDetails).slice(0,250));return r.result.value};const r=await ev("(()=>{const btn=document.querySelector(\".facet-btn\");const panels=[...document.querySelectorAll(\"[class*=facet-panel]\")].map(x=>x.className.slice(0,70));const bodies=[...document.querySelectorAll(\"[class*=facet-body]\")].map(x=>x.className.slice(0,70));return{btn:{exists:!!btn,display:btn?getComputedStyle(btn).display:null,visible:btn?btn.getBoundingClientRect().width>0:null,ariaExp:btn?btn.getAttribute(\"aria-expanded\"):null},panels,bodies,mobileMq:matchMedia(\"(max-width: 720px)\").matches}})()");console.log(JSON.stringify(r,null,1));ws.close();process.exit(0)