const CDP=9333;const l=await (await fetch("http://127.0.0.1:"+CDP+"/json/list")).json();const ws=new WebSocket(l.find(t=>t.type==="page").webSocketDebuggerUrl);await new Promise(r=>ws.onopen=r);let seq=0;const pend=new Map();ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.id&&pend.has(m.id)){const p=pend.get(m.id);pend.delete(m.id);m.error?p.rej(new Error(JSON.stringify(m.error))):p.res(m.result)}};const send=(m,p={})=>new Promise((res,rej)=>{const id=++seq;pend.set(id,{res,rej});ws.send(JSON.stringify({id,method:m,params:p}))});const ev=async e=>(await send("Runtime.evaluate",{expression:e,returnByValue:true})).result.value;await send("Page.enable");await send("Page.navigate",{url:"http://localhost:5173/"});await new Promise(r=>setTimeout(r,1800));const nav0=await ev("performance.getEntriesByType(\"navigation\").length");const a=await ev("(()=>{const a=[...document.querySelectorAll(\"a\")].find(x=>x.getAttribute(\"href\")===\"/demos\");if(a){a.click();return a.tagName+\"|\"+a.className.slice(0,40)}return null})()");await new Promise(r=>setTimeout(r,900));const nav1=await ev("performance.getEntriesByType(\"navigation\").length");const kids=await ev("(()=>{const main=document.querySelector(\"main\");return main?[...main.children].map(c=>c.tagName+\".\"+String(c.className).split(\" \").slice(0,2).join(\".\")):null})()");console.log(JSON.stringify({clicked:a,nav0,nav1,spa:nav1===nav0,kids}));ws.close();process.exit(0)