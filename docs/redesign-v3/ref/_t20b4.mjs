const CDP=9333;const l=await (await fetch("http://127.0.0.1:"+CDP+"/json/list")).json();const ws=new WebSocket(l.find(t=>t.type==="page").webSocketDebuggerUrl);await new Promise(r=>ws.onopen=r);let seq=0;const pend=new Map();ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.id&&pend.has(m.id)){const p=pend.get(m.id);pend.delete(m.id);m.error?p.rej(new Error(JSON.stringify(m.error))):p.res(m.result)}};const send=(m,p={})=>new Promise((res,rej)=>{const id=++seq;pend.set(id,{res,rej});ws.send(JSON.stringify({id,method:m,params:p}))});const ev=async e=>(await send("Runtime.evaluate",{expression:e,returnByValue:true,awaitPromise:true})).result.value;await send("Page.enable");await send("Page.navigate",{url:"http://localhost:5173/"});await new Promise(r=>setTimeout(r,1800));const r=await ev("(async () => { const seen=[]; const h=(e)=>{ if(e.animationName && !e.animationName.includes(\"march\")) seen.push(e.animationName+\"@\"+(e.target.className||e.target.tagName).toString().slice(0,40)) }; document.addEventListener(\"animationend\", h, true); const a=[...document.querySelectorAll(\"a\")].find(x=>x.getAttribute(\"href\")===\"/demos\"); a.click(); await new Promise(r=>setTimeout(r,900)); document.removeEventListener(\"animationend\", h, true); return { seen: seen.slice(0,6), path: location.pathname } })()");console.log(JSON.stringify(r));ws.close();process.exit(0)