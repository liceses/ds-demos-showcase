const CDP=9333;const l=await (await fetch("http://127.0.0.1:"+CDP+"/json/list")).json();const ws=new WebSocket(l.find(t=>t.type==="page").webSocketDebuggerUrl);await new Promise(r=>ws.onopen=r);let seq=0;const pend=new Map();ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.id&&pend.has(m.id)){const p=pend.get(m.id);pend.delete(m.id);m.error?p.rej(new Error(JSON.stringify(m.error))):p.res(m.result)}};const send=(m,p={})=>new Promise((res,rej)=>{const id=++seq;pend.set(id,{res,rej});ws.send(JSON.stringify({id,method:m,params:p}))});const ev=async e=>(await send("Runtime.evaluate",{expression:e,returnByValue:true})).result.value;await send("Page.navigate",{url:"http://localhost:5173/"});await new Promise(r=>setTimeout(r,1800));const r=await ev("(() => { const app=document.querySelector(\"#app\").__vue_app__; const root=app._instance; const names=[]; const walk=(v,d)=>{ if(!v||d>6)return; const t=v.type; const n=typeof t===\"object\"?(t.name||t.__name||\"anon\"):String(t).slice(0,20); names.push(\" \".repeat(d)+n+(v.transition?\" [TRANSITION]\":\"\")+(v.el&&v.el.nodeType===1?\" <\"+v.el.tagName+\">\":\"\")); if(v.component)walk(v.component.subTree,d+1); if(Array.isArray(v.children))v.children.forEach(c=>walk(c,d+1)); else if(v.children&&typeof v.children===\"object\")walk(v.children,d+1) }; walk(root.subTree,0); return names.slice(0,20) })()");console.log(JSON.stringify(r,null,1));ws.close();process.exit(0)