# 04 · 组件规范（七态矩阵）

> 每个组件都要回答**七态**：rest / hover / active / focus-visible / disabled / loading / `hover:none`（触屏），
> 外加 reduced-motion 与键盘可达性。**没写的状态 = 未定义**，不是"默认没问题"。
> 状态取值一律引令牌（`03-令牌表.generated.md`），不写 px 字面值。

## 4.1 矩阵模板

| 态 | 视觉 | 实现 |
|---|---|---|
| rest | 影 = `--lift-sh` | `.b-lift`（库） |
| hover | 抬起 `--lift-*` + 影 = `--lift-sh-hover` | `.b-lift:hover`（仅 `@media (hover:hover)`） |
| active | 位移 = `--lift-flat`、影清零、0ms 硬切 | `.b-lift:active` |
| focus-visible | `outline: 4px solid var(--ink); outline-offset: 2px` | 组件自带（库不管焦点） |
| disabled | 影清零 + `opacity` 降（`aria-disabled` 或原生 `disabled`） | 组件 |
| loading | 蚂蚁线（`.b-march`）或骨架屏；**不许禁用按钮但无反馈** | 组件 |
| `hover:none` | hover 物理不生效；反馈由 `:active` 承担 | 库的 `@media (hover:hover)` |

## 4.2 本轮已定义（阶段 1，实测值）

| 组件 | rest | hover | active | focus | 触屏 | reduce | 证据 |
|---|---|---|---|---|---|---|---|
| **实体卡 / 行卡**（`.card-entity` → `.explore-cell` `.model-row` `.task-row` `.compare-row`） | 3px 墨（`--shadow-entity`） | 5px 墨 + 抬起 2px | 位移 3px + 影清零 + ±1° 微倾 | 4px 焦点环 | hover 不生效，`:active` 反馈 | 过渡/位移退场 | 实测 3px→5px（墨黑 3px→5px 暖白） |
| **题目行**（`.task-line`） | 同上（**共用同一对令牌**） | 同上 | 同上 | 同上 | 同上 | 同上 | 实测 3px→5px；与卡片族门禁 ③ 绑定 |
| **小 chip**（`.model-chip`） | 2px 墨 | 3px 墨 + 抬起 1px | 位移 2px + 影清零 | 4px 焦点环 | 同上 | 同上 | 实测 2px→3px |
| **作品卡**（`.card-hover`） | 无影 | 12px 红 + 抬起 2px（Rule 04 同值） | 位移 12px + 影清零 | 4px 焦点环 | 同上 | 同上 | 实测 none→`rgb(255,107,107) 12px 12px` + `translate(-2px,-2px)` |
| **按钮**（`.btn` + 5 变体） | 无影 / 变体 8px | 抬起 2px（变体影保持 = 表观增影） | 位移 `--sh-off-md` + 影清零 + 微倾 | 4px 焦点环 | 同上（`.btn` 早就有守卫） | 同上 | 实测：变体 rest 8px、hover 8px+抬起；论坛壳内走 3px 轻量档 |
| **无影元素统一档**（`.ac-card` 后台卡 / `.uw-type` 上传类型卡 / `.pack-chip` / `.forum-topic-card`） | 无影 | **4px 档** + 抬起 1~2px | 位移 `--sh-off-md` + 影清零 | 4px 焦点环 | 库自动守卫 | 库自动 | 实测 hover `4px 4px`（原先分别是 6px / 6px / 3px 软影 / 4px 四种写法） |
| **通知行**（`.notif-item`） | 3px 墨 | 5px 墨 + 抬起 1px | 位移 3px + 影清零 | 4px 焦点环 | 库自动 | 库自动 | 本地无通知数据未实测（CSS 已收库） |
| **下一件作品卡**（`.dv-next-card`） | 无影 | **12px 红** + 换黄底（Rule 02/04） | 位移 12px + 影清零 | 4px 焦点环 | 库自动 | 库自动 | 实测 hover `rgb(255,107,107) 12px`、背景 `rgb(255,230,109)` |
| **抽屉关闭键**（`.peek-close`） | 无影 | hover 换红底（色切 0ms） | active 位移 4px + 影清零（原**没清零影**） | 4px 焦点环 | 库自动 | 库自动 | CSS 已收库 |

## 4.3 待补（阶段 2 逐组件填七态）

| 组件族 | 现状 | 待补 |
|---|---|---|
| 卡片族其余（`.card-default` 31 处、`.card-sunny`、`.card-mint`、`.card-coral`、`.astra-*`） | 只有 rest（影值已令牌化） | hover/active/focus 三态 |
| 后台（`.ac-card` ✅已接线、`.inbox-row`、`.kpi`、`.merge-col`） | `.ac-card` 已入库（4px 档）；其余仍是 hover 字面值 | 剩余三个的七态 |
| 上传向导（`.uw-type` ✅、`.pack-chip` ✅、`.uw-step`、`.uw-item`） | 两个已入库；`.uw-step/.uw-item` 仍是 hover/active 字面值 | 剩余两个 + 触屏 |
| 论坛（`.forum-topic-card` ✅、`.forum-reply`、`.forum-peek`、`.forum-demo-card`、`.forum-shell .btn`） | 同名已清零；题卡已入库 | 其余四个 + 论坛壳按钮 |
| 列表行（`.notif-item` ✅、`.peek-*`、`.tag-drawer-bar`） | 通知行已入库 | 其余 + 七态 |
| 表单（`input/select/textarea`、`.input`） | rest/聚焦有 | disabled/error/loading 态 |
| 空态与错误态（`EmptyBox`） | 已有组件（错误态带 `role=alert` + 重试出口） | 无 |

## 4.4 组件命名与文件纪律

1. 组件样式落在 `styles/components/<name>.css`（通用）或 `styles/pages/<page>.css`（页面专属）。
2. **同一选择器只许一个文件定义**（门禁①，交互选择器已强制；其余选择器按"谁改谁拆"逐步收）。
3. 层序即层叠：`tokens → base → layouts → components → pages`（`styles/index.css` 的 @import 顺序）。
   页面文件覆盖组件文件是允许的**语境覆盖**（如论坛壳减档），但必须用**语境前缀**（`.forum-shell .btn`），
   不许用裸选择器 —— 裸选择器会泄漏到全站（本轮实测：论坛的 3px 按钮档泄漏成了全站档位）。
