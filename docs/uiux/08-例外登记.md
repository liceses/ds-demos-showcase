# 08 · 例外登记（每条都有到期条件）

> 规则：**凡是"我们知道它不符合规范/与参考站不同"的地方，都必须在这里**。没有登记的例外 = 缺陷。
> 销账时请连同证据一起删行，并在 `log/` 记一笔。

| # | 例外 | 范围 | 为什么 | 到期条件（谁销账） |
|---|---|---|---|---|
| ~~E-1~~ | ~~按钮 hover 走"影保持 + 抬起"~~ | **已定稿（2026-09-13 用户拍板：保持现状）** | 升为规范条款 M4②（`06-微交互.md`），不再是例外 | 门禁⑨钉住：变体的 `--lift-sh-hover` 必须等于 `--lift-sh` |
| **E-2** | 调色板 5 强调色 ≠ 参考站 4 强调色（且色值不同：`--p-red #ff6b6b` vs 参考 `#ff006e`） | 全站 | 保留"俏皮野兽派"五色系与黄=唯一行动色纪律；参考站是更冷的荧光系 | 视觉方向复审时一并决定（不阻塞其它条款） |
| **E-3** | `--sh-off-sm`(2px) / `--sh-off-uw`(3px) **不随 @720 减半** | 令牌层 | `primitives.css` 既有约定：2px 已是下限、3px 是"档外具名档"，减半会看不出 | 新增更小档位时重审 |
| **E-4** | 抬起量 `--lift-x/y` = **-2px**，参考站按钮是 4px | 全站 | 全站 29 处现值统一为 -2px；改 4px 是一次全站位移跳变 | 若要贴参考站，改 `primitives.css` 一个值即可（需一次全站截图复核） |
| ~~E-5~~ | ~~`--fs-*`（12 个）/ `--sp-*`（13 个）字号间距表几乎无人使用~~ | **已完成（2026-09-13 用户拍板：收编）** | 约 1600 处等值替换（codemod + 档外白名单 + 门禁⑧）；`--fs-*`/`--sp-*` 消费者从 1 变成上千 | 新代码必须引令牌：门禁⑧拦"档内字面值"与"未登记的档外值" |
| **E-6** | 顶栏共享控件未达 44px：墨/EN 39px、`tag-chip` 40px | 顶栏组件 | 组件级改动（会影响全站顶栏高度与换行） | 与顶栏复审一起做（移动端触达专项） |
| **E-7** | 墨黑主题里 `--teal` 作为填充/描边约 40 处仍是电光青 `#00d9ff` | 墨黑主题 | 那是"参考站荧光系"的既定设计，不是漏改 | 参考站改版或墨黑观感复审时 |
| **E-8** | `.card-default` 的影仍是青（`--shadow-teal`，31 处），只有实体卡改成了墨 | 卡片族 | 用户本轮只要求模型卡（实体卡）改墨；默认卡是另一个决策面 | 用户提出"默认卡也改墨"时（改 `semantic.css` 一行 `--shadow-teal` 的绑定） |

## 待清理台账（不是例外，是债）

| 项 | 数字 | 处置 |
|---|---|---|
| 库外存量交互物理 | **36 处 / 16 文件**（阶段 1 起已迁 9 处） | 分批迁进 `.b-lift`；基线文件 `frontend/tests/fixtures/physics-baseline.json` 只收窄 |
| 跨文件同名交互选择器 | **0 组**（已清零） | 由门禁①持续拦新增；新增即测试红 |
| 无人使用的令牌 | **36 个**（见 `03-令牌表.generated.md` 末节） | 其中 `--sh-off-hov-md/lg/xl` 是本轮新增**待接线**（阶段 2 用）；其余按 E-5 及逐个复核处置 |
| `--shadow-yellow` / `--b-shadow-lg` / `--b-dur-count` / `--b-dur-shake` / `--tilt-rest` / `--border-w-sm` / `--k-ink-3` | 7 个 | 逐个确认：要么接上线，要么删（避免"看起来有语义其实没人用"） |

## 阶段 2 迁移登记项（规则①：「只搬已存在的态」）

迁移的判定红线：**入库会凭空引入原来没有的态 ⇒ 不迁移，登记在此**（补态属于 04 §4.3 的显式动作，且要写进七态矩阵）。

| 选择器 | 现状 | 为什么不迁 | 到期条件 |
|---|---|---|---|
| `.dv-rail:active` · `.dv-collapse:active` · `.uw-edit:active`（`tokens/motion.css` 一条三选择器规则） | active 压平 `translate(var(--sh-off-sm))` + 影清零；三者**均无 hover 物理** | 挂上 `.b-lift` 后库会给它们 `hover { transform: translate(--lift-x,--lift-y) }` —— 等于**新增一个 hover 抬起**，违反"只搬已存在的态" | D-3 补这三个组件的 hover 态时一并决定（届时要么给库变量、要么明确"hover 不抬起"） |
| `.uw-step:hover` | 只换底色（**刻意不抬起**），规则里写明注释 | 同上（本来就没有抬起的物理） | 同上（04 §4.3 上传向导补 hover 态时） |

> 注：这两项的**静止/按下**表现已合规（压平位移 = 同档偏移、影清零），只是"hover 抬起"这件事在原件里从未存在。
