# 2026-09-13 · 探索页改版收尾 + UI/UX 规范立规（阶段 1）

> 轮次记录（`log/` 只记"哪一轮做了什么、证据在哪"，规范条款在 `../` 各章）。
> 本轮的完整原始记录仍在 `docs/前端UIUX重设计方案.md`（保留不删，作为过程档）。

## 这一轮做了什么

1. **题目行接真实封面**（200px 缩略图管线）：`Demo.cover_thumb_url` + `storage.make_cover_thumb/cover_thumb_url` +
   `task_service.representative_covers`（与 `task_chain` 同序、跳过无缩略图作品）+ `scripts/backfill_cover_thumbs.py`。
   实测：8 张缩略图 **27.3KB**（原图 580KB）；移动端行高 81 → 103~107。
   证据：`docs/前端UIUX重设计方案.md §12`、静态对照页 `.tmp-cdp-profile/preview/*`（不入库）。
2. **微交互统一（本目录诞生的直接原因）**：用户问"这种微动效全站不统一吗？应该搞个公共库"。
   审计结论：29 处手写抬升 / 20 个文件 / 6 种影值 / 5 组跨文件同名覆盖 / 4 处"影缩" / 触屏守卫只有 1 处。
   → 建 `styles/components/lift.css`（`.b-lift`）+ hover 档 = 静止档 ×1.5 的令牌表 + 三条门禁 + 迁移基线。
3. **立规**：`docs/uiux/`（10 章）+ 两个生成脚本 + 12 条护栏；参考站四法则列为宪章层（2026-09-13 实抓复核）。

## 顺手修掉的存量缺陷（都写进了 `../06-微交互.md` 与 `../08-例外登记.md`）

| # | 现象 | 证据 |
|---|---|---|
| 1 | 论坛的"壳内按钮减档"是裸选择器 → 全站按钮影档被改成 3px | 实测 `.btn-primary` 静止 3px → 修后 8px |
| 2 | `.ac-card:hover` / `.uw-type:hover` 跨文件同名，各有一份从生效 | 读 `index.css` 导入顺序 + 两份规则体 |
| 3 | `.animate-in` 的 `fill-mode: both` 锁死 transform → 作品卡 hover 抬起从未生效 | 实测 hover 后 `transform: matrix(1,0,0,1,-2,-2)`（修前恒等） |

## 验收证据（本轮）

- 12 条新护栏全绿；故意写一条裸物理 → 门禁②变红（输出抓在验收页）
- 迁移后实测：模型格/模型行/题目行/对比行 = 3px → hover 5px（墨）；墨黑主题自动翻暖白 3px → 5px；chip 2px → 3px
- 验收页：`.tmp-cdp-profile/preview/uiux-spec.html`（自包含、零脚本、四态 1:1 截图）

## 下一轮（阶段 2）

`../04-组件.md` 4.3 的七态矩阵补全 + `../06-微交互.md` 6.7 的 45 处迁移 + `../09-规范↔门禁对照.md` 9.2 的 8 个缺口
（优先 D-2 WCAG 矩阵进 CI、D-4/D-6 stylelint 两条）。
